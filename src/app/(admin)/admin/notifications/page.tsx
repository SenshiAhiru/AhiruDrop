"use client";

// Admin notifications page — same schema as user notifications.
// Reuses the user endpoint since Notification is keyed by userId.
export { default } from "@/app/(dashboard)/dashboard/notifications/page";
